# Testing

## Commands
- Run tests: `npm test`
- Build check: `npm run build`
- Dev run: `npm start`

## Current baseline
- Vitest + Vue Test Utils are configured.
- Existing sample test: `src/app/App.spec.ts`.

## Proposed
- Proposed: add store unit tests for critical domains (`auth`, `tasks`) and route guard tests for auth flows.
